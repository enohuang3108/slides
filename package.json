{
  "name": "mono-slidev",
  "version": "1.0.0",
  "description": "A monorepo for multiple Slidev presentations with GitHub Pages deployment",
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "clean": "pnpm -r run clean && rm -rf dist",
    "lint": "pnpm -r run lint",
    "preview": "pnpm -r run preview"
  },
  "keywords": [
    "slidev",
    "presentations",
    "monorepo",
    "github-pages"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0",
  "dependencies": {
    "@slidev/cli": "^0.48.9",
    "@slidev/theme-default": "latest",
    "@slidev/theme-seriph": "latest",
    "@vueuse/core": "^10.0.0",
    "vue": "^3.3.0",
    "vite": "^7.0.5"
  },
  "devDependencies": {
    "playwright-chromium": "^1.40.0",
    "@iconify-json/carbon": "^1.1.0",
    "@iconify-json/mdi": "^1.1.0"
  }
}
