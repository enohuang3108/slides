{
  "name": "demo-basic",
  "version": "1.0.0",
  "description": "Basic Slidev presentation demo",
  "type": "module",
  "scripts": {
    "dev": "slidev --open",
    "build": "slidev build",
    "export": "slidev export",
    "preview": "vite preview --base /",
    "clean": "rm -rf dist"
  }
}
