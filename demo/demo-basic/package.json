{
  "type": "module",
  "scripts": {
    "dev": "slidev --open",
    "build": "slidev build --base /slides/demo/demo-basic/ --out ../../dist/demo/demo-basic",
    "export": "slidev export --per-slide --output demo-basic.pdf",
    "preview": "vite preview"
  }
}
